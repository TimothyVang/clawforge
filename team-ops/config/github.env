# GitHub identity for team-ops (non-secret)
# Token comes from `gh auth token` at runtime — never commit tokens.

GITHUB_OWNER=TimothyVang
GITHUB_VISIBILITY=private
GITHUB_META_REPO=team-ops
# Auth: already logged in via gh CLI (keyring) as TimothyVang
# scopes: gist, read:org, repo, workflow, write:packages
