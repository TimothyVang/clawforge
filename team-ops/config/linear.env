# Linear — team-ops project CANCELED (user request 2026-07-18)
# Team "PUGtest" still exists in workspace pugtest (only team; MCP cannot delete teams).
# It still holds older non-team-ops work (Rust-DFIR, etc.).
#
# To fully delete the team in Linear UI:
#   Settings → Teams → PUGtest → Delete team
#   (or delete workspace if you want everything gone)
#
# LINEAR_TEAM=
# LINEAR_PROJECT=
# LINEAR_PROJECT_URL=
LINEAR_WORKSPACE=
LINEAR_TEAM=
LINEAR_TEAM_ID=
LINEAR_TEAM_KEY=
LINEAR_PROJECT=
LINEAR_PROJECT_ID=
LINEAR_PROJECT_URL=
LINEAR_STATUS=removed-from-team-ops
